/* Seção 3: JavaScript - Lógica de programação
Vídeo nro. 54 
 árvore do DOM
 */

/*
elemento-mãe = window
segue a ele o document
e apartir disso todos são filhos (child)

    #document é o topo do dom (document é child de window)
        - segue ele o HTML (html é child de document)
            - segue o HTML o head e o body (body é child de html)
                - dentro de body está, por exemplo, section (body é parent de section)
                    - dentro de section está o h1 que é um texNode. 
*/

